# fuchsia-gradient

## How to use
```
In each input field, you can use hex code color to set the gradient in the background.
You can add or remove input fields by using the plus and minus buttons.
You can use the random button to generate random colors in the background and input fields.
You can lock a color by clicking the lock button on an input field.

```
